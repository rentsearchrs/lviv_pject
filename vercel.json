{   
    "builds":[
        {
        "src" : "main.py",
        "use" : "@vercel/python",
        "config": { "startCommand": "python3 -m venv venv && source ./venv/bin/activate && pip install -r requirements.txt && cd parser && pip install pysqlite3-binary && uvicorn main:app --host 0.0.0.0 --port 8000" }
        }
    ],
    "routes":[
        {
            "src" : "/{.*}",
            "dest": "main.py"
        }
    ]
    
}
