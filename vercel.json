{
    "version": 2,
    "builds": [
        {
            "src": "/parser/main.py",
            "use": "@vercel/python",
            "config": {
                "startCommand": "uvicorn main:app"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "parser.main:app",
            "methods": ["GET", "POST", "PUT", "DELETE", "PATCH", "OPTIONS"],
            "headers": {
              "Access-Control-Allow-Origin": "*"
            }
        }
    ]
}
      
